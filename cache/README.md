# Cache

`cache/` contains local cache files produced by runtime services and tooling.

Cache contents are machine-local and intentionally excluded from Git. This
descriptor preserves the repository layout without publishing generated data.
