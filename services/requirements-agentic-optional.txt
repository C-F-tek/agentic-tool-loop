# Optional public packages for agentic runtime and validation features.
# These are not imported by every default execution path; install them when
# enabling the corresponding optional service or validation capability.

# Token counting/budgeting before sending history/tool results to local models.
tiktoken

# Strict validation for planner decisions and tool envelopes.
jsonschema

# Retry/backoff wrapper for transient Ollama/FastAPI/network failures.
tenacity

# Optional faster JSON serialization for very large artifacts.
orjson
# Property-based validation for deterministic repository probe profiles.
hypothesis
