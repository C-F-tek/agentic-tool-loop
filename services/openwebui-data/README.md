# OpenWebUI Service Data

`services/openwebui-data/` is the service-local OpenWebUI data directory used by
the local runtime.

It can contain generated OpenWebUI state, local databases, uploads, caches, and
runtime files. These contents are managed locally and are intentionally excluded
from Git.

Only this descriptor is intended to be committed from this directory.
