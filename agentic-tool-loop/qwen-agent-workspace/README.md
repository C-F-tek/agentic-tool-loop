# Qwen Agent Workspace

`qwen-agent-workspace/` is the local workspace used by the agentic loop for job
state, tool results, planner streams, and generated artifacts.

Workspace contents are runtime data and are intentionally excluded from Git.
