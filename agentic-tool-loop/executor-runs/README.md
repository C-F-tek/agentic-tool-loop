# Executor Runs

`executor-runs/` stores local execution runs produced by executor services.

Run outputs, logs, temporary files, and per-run state are intentionally excluded
from Git. This descriptor keeps the directory role visible in the project tree.
