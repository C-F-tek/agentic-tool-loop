# Virtual Environments

`venvs/` contains local Python virtual environments used by the project and
runtime services.

Virtual environment contents are machine-local dependencies and are intentionally
excluded from Git.
