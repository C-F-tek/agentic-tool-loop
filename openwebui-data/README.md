# OpenWebUI Data

`openwebui-data/` is a local OpenWebUI data directory.

It can contain user chats, generated state, uploads, caches, runtime databases,
and other machine-local data managed by OpenWebUI. These contents are not part
of this repository's source package and are intentionally excluded from Git.

Only this descriptor is intended to be committed from this directory.
